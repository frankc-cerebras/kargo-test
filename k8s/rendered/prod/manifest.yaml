apiVersion: v1
data:
  index.html: |-
    <!DOCTYPE html>
    <html>
    <head>
    <title>prod</title>
    </head>
    <body>

    <h1>prod</h1>

    </body>
    </html>
kind: ConfigMap
metadata:
  name: kargo-test-content-prod
---
apiVersion: v1
kind: Service
metadata:
  name: kargo-test-prod
spec:
  ports:
  - nodePort: 32052
    port: 3000
    protocol: TCP
    targetPort: 80
  selector:
    app: kargo-test
  type: NodePort
---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: kargo-test-prod
spec:
  replicas: 1
  selector:
    matchLabels:
      app: kargo-test
  template:
    metadata:
      labels:
        app: kargo-test
    spec:
      containers:
      - image: public.ecr.aws/nginx/nginx:1.28
        name: nginx
        volumeMounts:
        - mountPath: /usr/share/nginx/html
          name: content
          readOnly: true
      volumes:
      - configMap:
          name: kargo-test-content-prod
        name: content
