apiVersion: v1
data:
  index.html: |-
    <!DOCTYPE html>
    <html>
    <head>
    <title>test</title>
    </head>
    <body>

    <h1>test</h1>

    </body>
    </html>
kind: ConfigMap
metadata:
  labels:
    app: kargo-test-test
  name: kargo-test-content-test
---
apiVersion: v1
kind: Service
metadata:
  labels:
    app: kargo-test-test
  name: kargo-test-test
spec:
  ports:
  - nodePort: 32051
    port: 3000
    protocol: TCP
    targetPort: 80
  selector:
    app: kargo-test-test
  type: NodePort
---
apiVersion: apps/v1
kind: Deployment
metadata:
  labels:
    app: kargo-test-test
  name: kargo-test-test
spec:
  replicas: 1
  selector:
    matchLabels:
      app: kargo-test-test
  template:
    metadata:
      labels:
        app: kargo-test-test
    spec:
      containers:
      - image: public.ecr.aws/nginx/nginx:1.28
        name: nginx
        volumeMounts:
        - mountPath: /usr/share/nginx/html
          name: content
          readOnly: true
      volumes:
      - configMap:
          name: kargo-test-content-test
        name: content
