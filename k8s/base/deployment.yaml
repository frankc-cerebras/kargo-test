apiVersion: apps/v1
kind: Deployment
metadata:
  name: kargo-test
spec:
  replicas: 1
  selector:
    matchLabels:
      app: kargo-test
  template:
    metadata:
      labels:
        app: kargo-test
    spec:
      containers:
      - name: nginx 
        image: nginx
        volumeMounts:
        - name: content
          mountPath: /usr/share/nginx/html
          readOnly: true
      volumes:
      - name: content
        configMap:
          name: kargo-test-content